<%= title(@page.title + " - " + current_subdomain_user.name + " - MinimalPixel") %>

<h1><%= @page.title %></h1>

<%= @page.content %>

<% if authorized? %>
	<%= link_to 'Edit', edit_page_path(@page) %> |
<% end -%>
<%= link_to 'Back', projects_root_path(:subdomain => current_subdomain) %>
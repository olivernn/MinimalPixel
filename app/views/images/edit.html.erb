<h1>Editing image</h1>

<% form_for([@project, @image]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', [@project, @image] %> |
<%= link_to 'Back', project_images_path(@project) %>

$("#item_view").hide().html("<%= escape_javascript(render_item(@item, @project)) %>").fadeIn();
// need to re_apply the sifr font for the image heading!
$("#item_view h1").sifr({font:'<%= @style.font.font.url %>', color:'<%= @style.heading_colour %>'});
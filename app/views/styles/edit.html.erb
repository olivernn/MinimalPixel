<h1>Editing style</h1>

<% form_for(@style) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => {:f => f } %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @style %> |
<%= link_to 'Back', styles_path %>

<h1>New style</h1>

<% form_for(@style) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form', :locals => {:f => f } %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', styles_path %>

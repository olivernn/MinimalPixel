<h1>Editing theme</h1>

<% form_for(@theme) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form', :locals => {:f => f } %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @theme %> |
<%= link_to 'Back', themes_path %>
